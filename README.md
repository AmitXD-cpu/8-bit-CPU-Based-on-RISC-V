In the instruction memory , I have added the assembly code for calculating the 1st 15 fibonacci numbers.

but the 15th fibonacci number won't be printed as it exceeds the number 255( which is the maximum number this cpu can store, as it operates with 8 bit register).

